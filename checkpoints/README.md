# Checkpoints

No trained model weights are included in this repository.

Run the training launchers to create checkpoints locally, or provide paths to checkpoints stored outside the repository. See [`../docs/CHECKPOINTS.md`](../docs/CHECKPOINTS.md) for the dependency chain and expected filenames.
