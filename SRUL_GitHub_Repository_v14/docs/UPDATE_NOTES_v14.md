# Update notes - v14

- The abstract now states the full motivation chain: autoencoder latents need not be Gaussian; normalized latents can form a fixed-radius shell; Gaussian radial variation and linear Euclidean paths are then mismatched; SRUL uses a spherical source and geodesic RFM transport.
- Slide 1 uses the same problem-to-solution logic and was re-laid out to avoid text overlap.
- The speaker script, README, and method documentation were updated for consistent terminology.
